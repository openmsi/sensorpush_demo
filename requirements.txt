# OpenMSIStream for streaming data
openmsistream >= 1.7.9
# OpenMSIToolbox for ControlledProcess
openmsitoolbox >= 1.2.0
# pandas for organizing data in dataframes
pandas >= 2.1.2
# pyarrow, as a future pandas dependency
pyarrow >= 15.0.0
# Python wrapper around the SensorPush Bluetooth API
sensorpush-bleak >= 1.0.7
